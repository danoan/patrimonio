# --- App settings (used directly by app/config.py) ---
# Local/dev default; docker-compose.yml overrides this to the in-container /data path.
DATABASE_URL=sqlite:///./patrimonio.db
TZ=Europe/Paris

# Host directory the SQLite DB is bind-mounted from (docker-compose.yml mounts it at /data).
DATA_DIR=./data
